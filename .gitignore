# Build products
.build/
DerivedData/
*.xcodeproj/xcuserdata/
.swiftpm/

# App bundle is generated by build-app.sh / xcodebuild — never commit it
Tebo.app/
*.dmg

# Bundled third-party engine binary (downloaded + hash-pinned by scripts/fetch-engine.sh)
Engines/czkawka_cli

# Release output (DMG + staging)
dist/

# Profiling / coverage artefacts
*.profraw
*.profdata

# macOS noise
.DS_Store
._*

# Local working notes: kept on disk, deliberately not published
.hermes/
.hermes.md
