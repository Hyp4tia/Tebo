# SuperClean default whitelist (one substring per line).
# Anything containing these strings is NEVER deleted.
# Copy to ~/.config/superclean/whitelist to customize (M2).

~/Library/Caches/com.apple.Safari
~/Library/Application Support/MobileSync
Photos Library.photoslibrary
